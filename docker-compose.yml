version: "3.9"

services:
  postgres-db:
    image: postgres:14
    container_name: postgres
    env_file:
      - postgres.env
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - app-network

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backend
    env_file:
      - backend.env
    depends_on:
      - postgres-db
    ports:
      - "3000:3000"
    networks:
      - app-network

networks:
  app-network:

volumes:
  pgdata:
